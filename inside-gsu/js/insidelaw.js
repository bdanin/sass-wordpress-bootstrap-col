jQuery.noConflict();
jQuery( document ).ready(function( $ ) {
	// trying to remove toggle classes that break the menu (bootstrap issues)
	// $('.nav-collapse a').removeAttr('class').removeAttr('data-toggle');
	// $('.nav-collapse li').removeClass('dropdown');

	
});